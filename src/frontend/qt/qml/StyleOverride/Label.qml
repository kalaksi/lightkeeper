import QtQuick
import QtQuick.Controls
// import org.kde.kirigami 2.19 as Kirigami


Label {
    // TODO: disabled for qt6, remove later
    // Kirigami.Theme.inherit: false
    // Kirigami.Theme.textColor: Theme.textColor
}