hosts:
  test1:
    fqdn: test1.example.invalid
    templates:
      - defaults
      - linux
      - docker
      - docker-compose
      - systemd-service
    monitors:
      ping:
        is_critical: true
  test2:
    address: 10.0.0.1
    monitors:
      ping:
        is_critical: true
